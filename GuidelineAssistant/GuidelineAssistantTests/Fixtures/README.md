Optional test fixtures:

- embedding_reference.json
  - Python reference embedding for the exact phrase:
    "neonatal sepsis early-onset"
  - Used by EmbeddingTests for cosine parity validation (> 0.999).
